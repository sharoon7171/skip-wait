Uses "storage" (chrome.storage.local) only for short-lived progress during multi-step shortener chains so the extension can continue a hop sequence across pages. That data stays on the device, is not sent to us, and is not used for ads or profiling. There is no global on/off setting stored here—the popup does not turn automation off.
