Uses "storage" (chrome.storage.local) for short-lived progress during multi-step shortener chains and for the cached supported-site list (hosts.json) plus its last refresh time. That data stays on the device, is not sent to us, and is not used for ads or profiling. There is no global on/off setting stored here—the popup does not turn automation off.
