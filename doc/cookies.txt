Uses the "cookies" permission so the background worker can read a site-set cookie on certain supported pages when the destination URL is stored only in an HttpOnly cookie. Content scripts cannot read HttpOnly cookies. Values are read locally to finish the redirect on those pages; they are not uploaded, shared, or used for ads. The extension does not set or delete cookies for this purpose.
